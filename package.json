{
  "name": "store",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun install --cwd backend && bun install --cwd admin && bun --cwd admin build && bun --cwd backend build",
    "start": "bun --cwd backend start"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
